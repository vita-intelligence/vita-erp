/**
 * Input — Vita ERP canonical import for HeroUI Input.
 *
 * Styling tokens (radius, border-width) are applied globally via globals.css
 * targeting the `.input` CSS class.
 *
 * Label weight (--vita-input-label-weight) is applied via the `.input` label
 * selector in globals.css.
 */

export * from "@heroui/react";
export {
  Input,
  type InputProps,
  InputRoot,
  type InputRootProps,
} from "@heroui/react";
