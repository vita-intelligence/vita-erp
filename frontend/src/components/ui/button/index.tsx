/**
 * Button — Vita ERP canonical import for HeroUI Button.
 *
 * Styling tokens (radius, border, font-weight, shadow, etc.) are applied
 * globally via globals.css targeting the `.button` CSS class.
 *
 * Available variants: primary | secondary | tertiary | ghost | outline | danger | danger-soft
 * Available sizes:    sm | md | lg
 */

export * from "@heroui/react";
export {
  Button,
  type ButtonProps,
  ButtonRoot,
  type ButtonRootProps,
} from "@heroui/react";
