/**
 * Re-export from canonical location for backwards compatibility.
 * Theme editor previews import from here; the actual hook lives in @/hooks.
 */
export { useCursorTrack } from "@/hooks/useCursorTrack";
