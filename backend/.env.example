SECRET_KEY=your-secret-key-here
DB_NAME=vita_erp
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# Stripe — get test keys from https://dashboard.stripe.com/test/apikeys
# Leave blank to boot without Stripe (local dev that doesn't touch billing).
# For webhook testing, run `stripe listen --forward-to localhost:8000/webhooks/stripe/`
# and copy the whsec_ value it prints into STRIPE_WEBHOOK_SECRET.
STRIPE_SECRET_KEY=
STRIPE_PUBLIC_KEY=
STRIPE_WEBHOOK_SECRET=
